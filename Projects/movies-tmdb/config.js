// Optional local configuration. Provider IDs can be numbers or strings.
const config = {
  providerFavorites: [
    8,    // Netflix
    337,  // Disney Plus
    9,    // Amazon Prime Video
  ],

  providerBlacklist: [
    // 0,  // Another service
  ],
};
